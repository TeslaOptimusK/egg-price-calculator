# 계란 알맹이 g당 단가 계산기 — 계산 스펙 v1

## 목적

사이즈(호수) + 가격 + 개수 → **껍질 제외 알맹이 기준 단가**로 비교.

## 호수(사이즈) — 한국 관행 중량

| 호수 | 1개 중량 범위 | 대표값(g) | 비고 |
|------|----------------|-----------|------|
| 왕란 | 68g 이상 | **70** | 범위 하한+여유 |
| 특란 | 60g 이상 ~ 68g 미만 | **64** | 중간값 |
| 대란 | 52g 이상 ~ 60g 미만 | **56** | 중간값 |
| 중란 | 44g 이상 ~ 52g 미만 | **48** | 중간값 |
| 소란 | 44g 미만 | **40** | 대표 소란 |

앱에서 **대표값 / 범위 하한(보수적) / 직접 입력** 선택 가능.

- 대표값: 위 표 (기본)
- 범위 하한: 왕란 68, 특란 60, 대란 52, 중란 44, 소란 36
- 직접 입력: 1개 실측 g

## 껍질 포함 / 제외 옵션

| 모드 | 사용 중량 | 설명 |
|------|-----------|------|
| **껍질 제외** (기본) | 전체 × α | 알맹이(흰자+노른자) g당 단가 |
| **껍질 포함** | 전체 × 1.0 | 호수 대표 중량 그대로 g당 단가 |

### 알맹이 비율 (껍질 제외 시)

```
알맹이 비율 α = 0.89   # 껍질 약 11% 가정 (기본)
허용 범위: 0.85 ~ 0.92 (슬라이더)
```

```
사용_g/개 = 1개 전체(g) × α   # 제외 시
사용_g/개 = 1개 전체(g)       # 포함 시
```

**고지:** α는 교재·개요 평균 추정치. 연구상 큰 알일수록 껍질 %가 다소 낮아지는 경향이 있으나 호수별 고정 표준은 아님.

## 단가 공식

```
총_사용_g  = 사용_g/개 × 개수
g당_원     = 가격_원 / 총_사용_g
10g당_원   = g당_원 × 10
개당_원    = 가격_원 / 개수
```

**메인 표시: 10g당 원** (체감용)

## 비교

A, B 각각 10g당 원 계산 후:

```
이득 = 더 비싼 쪽 10g당 − 더 싼 쪽 10g당
문구: "B가 10g당 N원 저렴" 등
```

## 기본 UI 값

- 개수 프리셋: 10, 15, 20, 30 (한판=30 강조)
- 사이즈 기본: 특란
- 가격: 빈칸 또는 최근 입력

## 가격 저장 (이력)

저장 시 기록:

| 필드 | 설명 |
|------|------|
| 시각 | 기기 로컬 시간 (ISO / epoch) |
| 위치 | GPS/네트워크 (선택, 거부 시 생략) |
| 지역 라벨 | 역지오코딩 (시·구 수준, 가능 시) |
| 메모 | 매장명 등 사용자 입력 |
| 계산 스냅샷 | 사이즈·개수·가격·10g당·옵션 |

- 웹: `localStorage` (최대 50건)
- Android: `SharedPreferences` JSON (최대 50건)
- **서버 전송 없음**

비교 탭에서 이력을 상품 A/B에 불러와 10g당 단가 비교 가능.

## 전국 참고 시세 의견

실시간 공시 API가 아니라 **앱 내 참고 팩가(30개)** 를 동일 공식으로 10g당 환산한 뒤 비교.

기본 팩가(원/30개, 수정 가능):

| 소란 | 중란 | 대란 | 특란 | 왕란 |
|------|------|------|------|------|
| 6200 | 7000 | 7800 | 8800 | 9800 |

의견 구간 (내 10g당 ÷ 시세 10g당):

| 차이 | 라벨 |
|------|------|
| ≤ −15% | 매우 저렴 |
| ≤ −5% | 저렴한 편 |
| −5% ~ +5% | 시세 수준 |
| &lt; +15% | 조금 비싼 편 |
| ≥ +15% | 비싼 편 |

웹에서 팩가·기준월·출처 수정 가능 (`localStorage`).

## 시세 갱신 정책 (하루 1회)

| 항목 | 내용 |
|------|------|
| 주기 | **한국 시간(Asia/Seoul) 기준 하루 1회** |
| 앱 시작 | 오늘 미갱신이면 1회 로드 후 `fetchDay` 기록 |
| 버튼 | 「오늘 시세 받기」— 이미 오늘 반영이면 비활성·안내 |
| 같은 날 재요청 | **네트워크 호출 없음** (로컬 캐시만) |
| 목적 | 서버/API 부하·비용 절감, 계란 소매에 충분한 신선도 |

서버(Workers 등)를 붙일 때도 동일하게 **서버 측 일 1~2회 KAMIS 캐시** + 앱 일 1회 수신을 권장.

## KAMIS 실시간 시세

| 항목 | 내용 |
|------|------|
| 발급 | https://www.kamis.or.kr/customer/reference/openapi_list.do |
| 저장 | 기기 로컬 (`p_cert_key`, `p_cert_id`) |
| API | `dailyPriceByCategoryList` 축산물(500) 소매 우선 |
| 매칭 | 품목명에 소란·중란·대란·특란·왕란 포함 시 해당 사이즈 |
| 환산 | 응답 단위(10개 등) → **30개 팩가** |
| 캐시 | 성공 시 6시간 |
| 웹 CORS | 기본 공개 프록시(allorigins) 옵션 |
| 비교 | **동일 sizeId만** 시세 의견에 사용 |

키가 없거나 실패 시 수동 참고 시세로 폴백.

## 저장 지점 지도

- 웹: Leaflet + OpenStreetMap (`이력·지도` 탭)
- Android: `MapActivity` WebView + 동일 HTML
- 필터: 전체 / 현재 사이즈만 / 사이즈 선택
- 마커 색: 소·중·대·특·왕 구분

## 같은 사이즈 비교 강화

- 토글 **같은 사이즈만** (기본 ON)
- 이력 불러오기 목록을 focus 사이즈로 필터
- A/B 사이즈 불일치 시 경고, ON이면 승자·시세 비교 보류
- 시세 의견 문구에 호수명 명시 (`특란 실시간 시세 대비…`)

## 고지

대표 중량·껍질 비율은 추정치. 실제 개당 중량·껍질은 제품·농장마다 다를 수 있음.
시세 의견·저장 위치는 참고용이며 투자·공시 데이터가 아님.
